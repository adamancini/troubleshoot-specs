apiVersion: troubleshoot.sh/v1beta2
kind: SupportBundle
metadata:
  name: cluster-down
spec:
  hostCollectors:
    - run:
        collectorName: "cni"
        command: "tail"
        args: ["-v", "-n", "+1", "/etc/cni/net.d/*"]
